headless:
  children:
    homeserver:
      hosts:
        dm-poepperl.de:
                vars:
        is_backup_sink: false
    backup:
      hosts:
        backup-dmp-main:
      vars:
        is_backup_sink: true
