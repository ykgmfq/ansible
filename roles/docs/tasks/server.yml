---
- name: Facts set
  set_fact:
    dir: "/opt/container/{{ role_name }}"
    volumes:
      zfs:
        - docs-db
        - docs
      local:
        - docs-redis
        - docs-thumbnails
- import_tasks: files.yml
- import_tasks: service.yml
- import_tasks: volumes.yml
