[Unit]
Description=Image build
After = network-online.target
Requires = network-online.target

[Service]
WorkingDirectory = /opt/services/images/%i
ExecStart = /usr/bin/fish build.fish
Restart=on-failure
RestartSec=1h
Type = oneshot
