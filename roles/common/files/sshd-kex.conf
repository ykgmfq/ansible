# Managed by Ansible (roles/common) -- do not edit on the host.
# Post-quantum key exchange preferred, with a Curve25519 fallback for older
# clients (e.g. the PuTTY-based Shelly iOS client, which lacks ML-KEM).
KexAlgorithms mlkem768x25519-sha256,curve25519-sha256,curve25519-sha256@libssh.org
