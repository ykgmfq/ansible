---
- name: User id set
  set_fact:
    id: "{{ users.cloud }}"
    db_pw: "{{ secrets.cloud.db_pw }}"
- import_tasks: ../../shared/user/main.yml
- import_tasks: server.yml
  when: not is_backup_system
- import_tasks: ../../shared/backup-sink/main.yml
  when: is_backup_system
