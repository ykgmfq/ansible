---
- name: Common headless functionality present
  hosts: headless
  remote_user: root
  roles:
    - apt
    - common
    - vim
    - backup_base
- name: Service providers
  hosts: homeserver
  remote_user: root
  roles:
    - container
    - home
    - media
